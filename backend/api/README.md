# Steps to install backend


`$ python3 -m venv venv`  
`$ source venv/bin/activate`  
`$ pip install django`  
`$ pip install pandas`  
`$ pip install scikit-learn`  

# Run the backend

`$ gunicorn api.wsgi`

